# Core dependencies for 3D Pulmonary Nodule Segmentation on LIDC-IDRI
# Compatible with PyTorch 2.x and CUDA 12.x
torch>=2.0.0
monai>=1.3.0
h5py>=3.0
numpy>=1.21
scipy>=1.7
scikit-learn>=1.0
tqdm>=4.60
pandas>=1.3
matplotlib>=3.4

# ---------- Dataset Extraction (local workstation only) ----------
# The following packages are only required for the initial LIDC-IDRI
# patch extraction step (build_hdf5.py) and should be run on a machine
# with internet access and the raw DICOM data. They are NOT needed
# for training or inference on the cluster.
pylidc>=0.2.0
pydicom>=2.3

#   ---------- CUDA Note ----------
#   This project was developed and tested with:
#   PyTorch 2.11.0 + CUDA 12.8 + cuDNN 9
#   For GPU support, install the appropriate PyTorch wheel for your CUDA version:
#   https://pytorch.org/get-started/locally/